--------------------------------------------------------------------------------

# СОСТОЯНИЕ 0: Инициализация

**Статическая память: R: [0,0,0] P: [0,0,0] Стэк: [ main ]**

Точка входа в программу. На этом этапе определяются константы, выделяется память под массивы и подготавливается рабочее окружение.

<pre>
#include &lt;iostream&gt;
using namespace std;

int main() {
    const int n = 3;
<b>---> int P[3]; int R[3] = {0,0,0}; </b>

    for (int i1 = 0; i1 < n; i1++) {
        P[i1] = i1; R[i1] = 1;
        for (int i2 = 0; i2 < n; i2++) {
            if (R[i2] == 0) {
                P[i2] = i2; R[i2] = 1;
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) {
                        P[i3] = i3; R[i3] = 1;
                        cout << P[0] << " " << P[1] << " " << P[2] << endl;
                        R[i3] = 0;
                    }
                }
                R[i2] = 0;
            }
        }
        R[i1] = 0;
    }
    return 0;
}
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 1: i1 = 0

**Статическая память: R: [1,0,0] P: [0,?,?] Стэк: [ main → i1=0 ]**

Алгоритм заходит в первый цикл. На первую позицию (P[0]) записывается число 0. В массиве R ставится отметка «занято» для этого индекса.

<pre>
    for (int i1 = 0; i1 < n; i1++) {
<b>--->    P[i1] = i1; R[i1] = 1;          // Заняли индекс 0 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 2: i2 = 1

**Статическая память: R: [1,1,0] P: [0,1,?] Стэк: [ main → i1=0 → i2=1 ]**

Программа переходит ко второму циклу. Так как 0 уже занят, ищется следующее свободное число (индекс 1). Оно фиксируется в P[1].

<pre>
        for (int i2 = 0; i2 < n; i2++) {
            if (R[i2] == 0) {               
<b>--->            P[i2] = i2; R[i2] = 1;      // Заняли индекс 1 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 3: i3 = 2 (ВЫВОД №1)

**Статическая память: R: [1,1,1] P: [0,1,2] Стэк: [ main → i1=0 → i2=1 → i3=2 ]**

На глубоком уровне найдена цифра 2. Выводится первая перестановка. Сразу после этого выполняется **бэктрекинг** для i3.

<pre>
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) { 
                        P[i3] = i3; R[i3] = 1; 
<b>--->                    cout << P[0] << " " << P[1] << " " << P[2] << endl; </b>
                        R[i3] = 0;          // BACKTRACKING: освободили число 2
                    }
                }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 4: i2 = 2 (Backtracking уровня i2)

**Статическая память: R: [1,0,1] P: [0,?,2] Стэк: [ main → i1=0 → i2=2 ]**

**Бэктрекинг** в действии: индекс 1 был освобожден (`R[i2]=0`), и цикл `i2` перешел к следующему значению 2.

<pre>
                // ... выход из предыдущего шага i2=1
                R[i2] = 0;                  // BACKTRACKING: освободили число 1
            }
<b>--->    for (int i2 = 0; i2 < n; i2++) {    // Цикл i2 продолжается, i2 стал 2 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 5: i3 = 1 (ВЫВОД №2)

**Статическая память: R: [1,1,1] P: [0,2,1] Стэк: [ main → i1=0 → i2=2 → i3=1 ]**

Благодаря бэктрекингу на уровне `i2`, теперь мы можем использовать число 1 на уровне `i3`. Выводится вторая комбинация: 0 2 1.

<pre>
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) {       
                        P[i3] = i3; R[i3] = 1; 
<b>--->                    cout << P[0] << " " << P[1] << " " << P[2] << endl; </b>
                        R[i3] = 0;          // BACKTRACKING: освободили число 1
                    }
                }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 6: i1 = 1 (Глобальный Backtracking)

**Статическая память: R: [0,1,0] P: [?,1,?] Стэк: [ main → i1=1 ]**

Завершен перебор всех вариантов для `i1=0`. Происходит **бэктрекинг** самого верхнего уровня: `R[0]` становится свободным, а `i1` переходит к 1.

<pre>
        R[i1] = 0;                          // GLOBAL BACKTRACKING: освободили число 0
    }
<b>---> for (int i1 = 0; i1 < n; i1++) {       // Внешний цикл перешел к i1=1 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 7: i2 = 0, i3 = 2 (ТРЕТИЙ ВЫВОД)

**Статическая память: R: [1,1,1] P: [1,0,2] Стэк: [ main → i1=1 → i2=0 → i3=2 ]**

Новая ветка. Поскольку 0 был освобожден бэктрекингом в Состоянии 6, мы можем поставить его на второе место. Печать: 1 0 2.

<pre>
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) {       
                        P[i3] = i3; R[i3] = 1; 
<b>--->                    cout << P[0] << " " << P[1] << " " << P[2] << endl; </b>
                        R[i3] = 0;          // BACKTRACKING
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 8: i1 = 2

**Статическая память: R: [0,0,1] P: [?,?,2] Стэк: [ main → i1=2 ]**

Очередной глобальный бэктрекинг: завершена ветка единицы. Теперь на первом месте фиксируется двойка. Числа 0 и 1 свободны.

<pre>
        R[i1] = 0;                          // BACKTRACKING уровня i1
    }
<b>---> for (int i1 = 0; i1 < n; i1++) {       // Фиксация i1=2 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 9: i2 = 0, i3 = 1 (ПОСЛЕДНИЙ ВЫВОД)

**Статическая память: R: [1,1,1] P: [2,0,1] Стэк: [ main → i1=2 → i2=0 → i3=1 ]**

Финальная стадия. Бэктрекинг позволил нам переиспользовать все числа для формирования последней группы перестановок. Вывод: 2 0 1.

<pre>
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) {       
                        P[i3] = i3; R[i3] = 1; 
<b>--->                    cout << P[0] << " " << P[1] << " " << P[2] << endl; </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 10: Конец работы

**Статическая память: R: [0,0,0] P: [2,0,1] Стэк: [ main ]**

Все циклы завершены. Последняя команда `R[i1] = 0` выполнила финальный бэктрекинг. Программа успешно завершена.

<pre>
        R[i1] = 0;
    }
<b>---> return 0;                              // Конец программы </b>
}
</pre>

--------------------------------------------------------------------------------