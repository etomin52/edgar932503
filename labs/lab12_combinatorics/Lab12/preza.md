--------------------------------------------------------------------------------

# СОСТОЯНИЕ 0: Инициализация

**Статическая память: R: [0,0,0] P: [?, ?, ?] Стэк: [ main ]**

Точка входа. Определение константы `n=3` и обнуление массива флагов `R`.

<pre>
#include &lt;iostream&gt;
using namespace std;

int main() {
    const int n = 3;
    int P[n], R[n] = {0};

    for (int i1 = 0; i1 < n; i1++) {
        P[0] = i1; R[i1] = 1;
        for (int i2 = 0; i2 < n; i2++) {
            if (R[i2] == 0) {
                P[1] = i2; R[i2] = 1;
                for (int i3 = 0; i3 < n; i3++) {
                    if (R[i3] == 0) {
                        P[2] = i3; R[i3] = 1;
                        cout << P[0]+1 << " " << P[1]+1 << " " << P[2]+1 << endl;
                        R[i3] = 0;
<b>--->                    if (i1 == 1) return 0; </b>
                    }
                }
                R[i2] = 0;
            }
        }
        R[i1] = 0;
    }
    return 0;
}
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 1: i1 = 0

**Статическая память: R: [1,0,0] P: [0, ?, ?] Стэк: [ main → i1=0 ]**

Алгоритм заходит в первый цикл. На позицию `P[0]` записывается 0. Массив флагов `R[0]` блокируется.

<pre>
    for (int i1 = 0; i1 < n; i1++) {
<b>--->    P[0] = i1; R[i1] = 1;               // P[0] = 0 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 2: i2 = 1

**Статическая память: R: [1,1,0] P: [0, 1, ?] Стэк: [ main → i1=0 → i2=1 ]**

Второй цикл находит свободный индекс 1. Теперь `P = [0, 1, ?]`. Числа 1 и 2 в массиве `R` помечены как занятые.

<pre>
        for (int i2 = 0; i2 < n; i2++) {
            if (R[i2] == 0) {
<b>--->            P[1] = i2; R[i2] = 1;       // P[1] = 1 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 3: i3 = 2 (ВЫВОД №1: 1 2 3)

**Статическая память: R: [1,1,1] P: [0, 1, 2] Стэк: [ main → i1=0 → i2=1 → i3=2 ]**

Третий цикл находит индекс 2. На экране появляется первая перестановка. Условие `if (i1 == 1)` ложно, работа продолжается.

<pre>
                if (R[i3] == 0) {
                    P[2] = i3; R[i3] = 1;
<b>--->                cout << P[0]+1 << " " << P[1]+1 << " " << P[2]+1 << endl; // 1 2 3 </b>
                }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 4: Backtracking i3

**Статическая память: R: [1,1,0] P: [0, 1, 2] Стэк: [ main → i1=0 → i2=1 ]**

После вывода число 3 (индекс 2) освобождается. Стэк возвращается на уровень цикла `i2`.

<pre>
<b>--->                R[i3] = 0;              // Число 3 свободно </b>
                }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 5: Backtracking i2

**Статическая память: R: [1,0,0] P: [0, 1, 2] Стэк: [ main → i1=0 ]**

Завершена итерация для `i2=1`. Число 2 освобождается. Цикл `i2` переходит к следующему шагу — поиску нового значения.

<pre>
<b>--->            R[i2] = 0;                  // Число 2 свободно </b>
            }
        }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 6: i2 = 2

**Статическая память: R: [1,0,1] P: [0, 2, ?] Стэк: [ main → i1=0 → i2=2 ]**

Цикл `i2` находит индекс 2 (число 3). Теперь `P[1] = 2`. Числа 1 и 3 заняты, число 2 — свободно.

<pre>
        for (int i2 = 0; i2 < n; i2++) {
            if (R[i2] == 0) {
<b>--->            P[1] = i2; R[i2] = 1;       // P[1] = 2 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 7: i3 = 1 (ВЫВОД №2: 1 3 2)

**Статическая память: R: [1,1,1] P: [0, 2, 1] Стэк: [ main → i1=0 → i2=2 → i3=1 ]**

Единственное свободное число 2 попадает в `P[2]`. На экране вторая строка: **1 3 2**.

<pre>
                if (R[i3] == 0) {
                    P[2] = i3; R[i3] = 1;
<b>--->                cout << P[0]+1 << " " << P[1]+1 << " " << P[2]+1 << endl; // 1 3 2 </b>
                }
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 8: Global Backtracking (i1 = 1)

**Статическая память: R: [0,1,0] P: [1, ?, ?] Стэк: [ main → i1=1 ]**

Ветка единицы завершена. `R[0]` освобождается. Внешний цикл переходит к `i1=1`. Теперь первая цифра — 2.

<pre>
<b>--->    R[i1] = 0;                          // Глобальный откат </b>
    }
    for (int i1 = 0; i1 < n; i1++) {        // i1 становится 1
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 9: i2 = 0, i3 = 2 (ВЫВОД №3: 2 1 3)

**Статическая память: R: [1,1,1] P: [1, 0, 2] Стэк: [ main → i1=1 → i2=0 → i3=2 ]**

Для новой ветки на второе место встает освобожденный ранее 0. На третье — 2. Печать: **2 1 3**.

<pre>
                if (R[i3] == 0) {
                    P[2] = i3; R[i3] = 1;
<b>--->                cout << P[0]+1 << " " << P[1]+1 << " " << P[2]+1 << endl; // 2 1 3 </b>
</pre>

--------------------------------------------------------------------------------

# СОСТОЯНИЕ 10: Завершение работы

**Статическая память: R: [0,0,0] P: [1, 0, 2] Стэк: [ main ]**

Срабатывает условие `if (i1 == 1) return 0`. Программа мгновенно прекращает работу. Все ресурсы освобождены.

<pre>
<b>--->                if (i1 == 1) return 0;  // Мгновенный выход </b>
                }
</pre>

--------------------------------------------------------------------------------